{
    "schemaVersion": 1,
    "id": "${id}",
    "version": "${version}",
    "name": "${name}",
    "description": "${description}",
    "authors": [
        "${author}"
    ],
    "contact": {
        "homepage": "${source}",
        "sources": "${source}",
        "issues": "${source}/issues"
    },
    "license": "MIT",
    "icon": "assets/${id}/icon.png",
    "environment": "*",
    "entrypoints": {
        "main": [
            {
                "adapter": "kotlin",
                "value": "${group}.EntrypointKt::init"
            }
        ],
        "fabric-datagen": [
            {
                "adapter": "kotlin",
                "value": "${group}.DataGenerator"
            }
        ],
        "mixinsquared": [
            "settingdust.the_abyss.TheAbyssMixinCanceller"
        ]
    },
    "mixins": [
        "${id}.mixins.json"
    ]
}
